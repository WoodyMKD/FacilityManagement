@model Modal
@{
    var deleteModalClass = "modal-confirm";
}

<div aria-hidden="true" role="dialog" tabindex="-1" id="@Model.ID" class="modal fade">
    <div class="modal-dialog @Model.ModalSizeClass @if(Model.IsDeleteModal) { @deleteModalClass } ">
        <div class="modal-content">

        </div>
    </div>
</div>

